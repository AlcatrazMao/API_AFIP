{
  "name": "afip-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker - API Gateway para AFIP",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240620.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}